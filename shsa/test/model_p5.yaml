# Checks if engines can handle graphs where no solution can be found.

relations:
  r1:
    a:
      in: [b, c]
      fct: ""
    b:
      in: [a, c]
      fct: ""


properties:

  # type will be generated, however 'properties' and at least 1 key have to be
  # available

  provided:
    a: False
    b: False
    c: False
